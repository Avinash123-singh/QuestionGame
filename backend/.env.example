PORT=5002
CLIENT_URL=http://localhost:5173,http://localhost:5174
NODE_ENV=development

# REQUIRED for production (Google live) — Supabase or Railway PostgreSQL
# Stores: questions (3k–10k per category), player profiles, seen-question history
# DATABASE_URL=postgresql://user:password@host:5432/dbname
